{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "NODE_OPTIONS=--experimental-vm-modules NODE_ENV=test JWT_SECRET=test-secret-key jest --runInBand --detectOpenHandles",
    "test:watch": "NODE_OPTIONS=--experimental-vm-modules NODE_ENV=test JWT_SECRET=test-secret-key jest --watch --runInBand",
    "test:coverage": "NODE_OPTIONS=--experimental-vm-modules NODE_ENV=test JWT_SECRET=test-secret-key jest --coverage --runInBand",
    "test:models": "NODE_OPTIONS=--experimental-vm-modules NODE_ENV=test JWT_SECRET=test-secret-key jest --testPathPatterns=models --runInBand",
    "test:controllers": "NODE_OPTIONS=--experimental-vm-modules NODE_ENV=test JWT_SECRET=test-secret-key jest --testPathPatterns=controllers --runInBand",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^9.0.0",
    "validator": "^13.15.23"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/supertest": "^6.0.3",
    "jest": "^30.2.0",
    "mongodb-memory-server": "^10.4.1",
    "nodemon": "^3.1.7",
    "supertest": "^7.1.4"
  }
}
